
# Symlink config files
sh "${PWD}/scripts/link-files.sh"

# Install Homebrew and dependencies
sh "${PWD}/homebrew/install.sh"

# Install Oh my zsh
sh "${PWD}/zsh/install.sh"

# Setup macOS
sh "${PWD}/macos/update.sh"
sh "${PWD}/macos/setup.sh"

# Setup Xcode
sh "${PWD}/xcode/setup.sh"
